
  <script type="text/javascript">
    var map;
    var layer_0;
    var layer_1;
    function initialize() {
      google.maps.visualRefresh = true;
      var isMobile = (navigator.userAgent.toLowerCase().indexOf('android') > -1) ||
      (navigator.userAgent.match(/(iPod|iPhone|iPad|BlackBerry|Windows Phone|iemobile)/));
      if (isMobile) {
        var viewport = document.querySelector("meta[name=viewport]");
        viewport.setAttribute('content', 'initial-scale=1.0, user-scalable=no');
      }
      var mapDiv = document.getElementById('map-canvas');
      mapDiv.style.width = isMobile ? '100%' : '100%';
      mapDiv.style.height = isMobile ? '540px' : '540px';
      map = new google.maps.Map(mapDiv, {
        center: new google.maps.LatLng(40.7399903, -73.9901026),
        zoom: 14,
        mapTypeId: google.maps.MapTypeId.ROADMAP
      });
      layer_1 = new google.maps.FusionTablesLayer({
        query: {
          select: "col8",
          from: "1lhzj737PB2NgR3CU5iC2NyXWHsfcZP7hO8ohNN0"
        },
        map: map,
        styleId: 2,
        templateId: 2
      });
    }
    function changeMap_1() {
      var whereClause;
      var searchString = document.getElementById('search-string_1').value.replace(/'/g, "\\'");
      if (searchString != '--Select--') {
        whereClause = "'Name' CONTAINS IGNORING CASE '" + searchString + "'";
      }
      layer_1.setOptions({
        query: {
          select: "col8",
          from: "1lhzj737PB2NgR3CU5iC2NyXWHsfcZP7hO8ohNN0",
          where: whereClause
        }
      });
    }
    google.maps.event.addDomListener(window, 'load', initialize);
  </script>